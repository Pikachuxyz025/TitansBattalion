using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Testing : MonoBehaviour
{
   public int jessie = 0, james = 0;
    public bool don;
    // Start is called before the first frame update
    void Start()
    {
        //don = true;
    }

    // Update is called once per frame
    void Update()
    {
        do
        {
            james++;
        }
        while (true); 
        //Debug.Log(don);
    }
}
